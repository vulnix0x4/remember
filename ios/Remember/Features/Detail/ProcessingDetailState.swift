import SwiftUI

struct ProcessingDetailState: View {
    var body: some View {
        RememberEmptyState(
            systemImage: "clock",
            title: "Getting this ready",
            message: "Your link is safe. Ideas show up here soon."
        )
    }
}
