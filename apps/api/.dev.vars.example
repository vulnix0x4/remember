# Copy to .dev.vars. Never commit real secrets.
AUTH_TOKEN=replace-with-a-long-random-token
# Required for Jev planning and when ANALYSIS_PROVIDER=openrouter.
OPENROUTER_API_KEY=replace-with-a-private-openrouter-key
# OPENROUTER_API_KEY also powers Jev task planning through OpenRouter Decisions.
