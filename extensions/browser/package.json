{
  "name": "@remember/browser-extension",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node --check background.js && node --check popup.js && node --check lib/api.mjs",
    "test": "node --test tests/*.test.mjs",
    "typecheck": "node --check background.js && node --check popup.js && node --check lib/api.mjs"
  }
}
